import React from 'react'

const CartWidget = () => {
    return (
        <div className='cart-widget'>
            <i className="cart-icon">ACÁ ESTÁ EL CARRITO</i>
        </div>
    );
};

export default CartWidget;